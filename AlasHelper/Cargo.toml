[package]
name = "alas-helper"
version = "0.3.0"
edition = "2024"
publish = false

[dependencies]
notify = "8.2.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
codegen-units = 1
lto = true
panic = "abort"
strip = "symbols"
